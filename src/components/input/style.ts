import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  inputStyle: {
    borderColor: "#D3D0CD",
    borderWidth: 1,
    borderRadius: 40,
    padding: 10,
    width: 300,
    backgroundColor: "#F1F7FA",
  },
});
